# Experiment entry points

All twelve numerical experiments now have data bundles and dedicated guides.
Use the [main experiment index](../../README.md) and
`uv run python scripts/reproduce.py list` for the current release scope.
This directory is retained as a compatibility link for earlier releases.
